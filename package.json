{
  "name": "gpt-midea-image-vault",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Image Vault para guardar imagens e expô-las a clientes MCP.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "@modelcontextprotocol/node": "^2.0.0",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "zod": "^4.1.5"
  }
}
